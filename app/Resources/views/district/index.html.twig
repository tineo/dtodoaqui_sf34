{% extends 'base.html.twig' %}

{% block body %}
    <h1>Districts list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Lat_lng</th>
                <th>Zoom</th>
                <th>State</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for district in districts %}
            <tr>
                <td><a href="{{ path('admin_district_show', { 'id': district.id }) }}">{{ district.id }}</a></td>
                <td>{{ district.name }}</td>
                <td>{{ district.latlng }}</td>
                <td>{{ district.zoom }}</td>
                <td>{{ district.state }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_district_show', { 'id': district.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_district_edit', { 'id': district.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_district_new') }}">Create a new district</a>
        </li>
    </ul>
{% endblock %}
